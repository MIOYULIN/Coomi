# Coomi Android v1.4.5 更新说明【稳定】

## 核心能力

- 新增改码审查协作模式：coder 模型负责修改，reviewer 模型只读审查，支持 1-3 轮自动修复。
- 新增 coder/reviewer 专用提示词、模型选择、测试证据审查开关和协作进度显示。
- `skill-creator` 作为内置 Skill 默认启用，用户可手动关闭但不可卸载。
- Tavily、Serena、GitHub 加入 MCP 推荐目录。

## Provider 与 Agent 稳定性

- Base URL 原样保存，供应商预设控制 `/v1` 兼容后缀，修复智谱 Coding URL 被错误追加 `/v1`。
- `/models` 拉取失败或返回空列表时允许手动输入模型 ID，避免阻塞火山方舟等 Provider。
- 修复流式 tool call 缺少函数名时显示 `unknown`、产生伪工具卡和重复纠正请求的问题。
- 支持标准、扁平化及 legacy tool call 形态，并增强流式响应错误诊断。
- 增加首 token 延迟、输出速度和本轮总 token 统计。

## 会话与界面

- 修复每个会话独立配置模型并持久化的问题。
- 顶部模型选择栏改为按供应商分组。
- 修复纯文字、虚拟列表和工具插入场景下的自动触底与滚动跟随。
- 全局置顶会话新增清空会话数据功能，保留标题、置顶、模型和模式。
- 数字生命体人格提示词增加示例格式并限制为 4000 字。

## Android 后台体验

- 后台/长任务显示标准任务通知，通知点击可回到对应会话。
- 支持后台任务运行时优先进入画中画；厂商不支持时使用通知降级。

