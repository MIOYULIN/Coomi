# Coomi Android v1.3.0

## 重要更新

- 新增核心层与普通层持久记忆、命中衰减和控制台手动管理；内建 `/memory` 与第三方记忆拓展严格隔离。
- 工具调用连续失败时先征得用户同意，再轻量调用一次当前模型生成脱敏优化报告并自动上传，不包含用户对话、文件内容、真实路径或密钥。
- 批量问询支持一次最多五个问题、跳过、返回重选和统一提交；新增 `/compact` 主动上下文压缩。
- 新增自定义钩子配置、参数说明与样例；身份定位上限提升至 20000 字。
- 提供商配置恢复“设为当前”，修复会话标题、置顶状态及多个二级页面的系统返回逻辑。
- 修复文件管理路径越界误判；备份覆盖自定义 MCP/SKILL 数据，并通过 Android 系统“另存为”导出。
- 图片预览支持手势缩放；优化 Root/Shizuku 权限引导、用户协议与隐私政策确认。
- 拓展广场清单支持本地缓存和启动更新；新增匿名日活统计及反馈后台数据展示。

## 下载

- `Coomi-Android-arm64-v1.3.0.apk`
- 架构：ARM64 (`arm64-v8a`)
- 系统要求：Android 7.0 及以上
