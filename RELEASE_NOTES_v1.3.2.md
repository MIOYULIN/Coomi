# Coomi Android v1.3.2

## 稳定版更新

- 多任务并行上限提升至 5 条，新增下载任务状态和置顶显示；依赖下载期间可继续处理不依赖该工具的任务。
- 修复“连接与重试”参数校验和保存逻辑，重连次数、首次间隔与最大间隔可正常配置并立即重新连接。
- 修复持久记忆删除按钮无效，增加明确的删除确认和执行状态。
- 重构手动与自动备份流程，增加阶段、百分比、失败原因和完成状态提示，避免重复启动备份任务。
- Provider 遇到模型能力不支持错误时自动降级可选能力并恢复请求，避免会话被 HTTP 400 直接终止。
- MCP 地址、安装状态或配置变更后立即热重载，无需重启引擎或新建会话。
- Agent 在非简单任务开始前主动检查已安装 Skill，并读取、遵循相关 Skill 指令。
- 优化并发任务、会话恢复和任务列表状态同步，提升长时间任务与后台执行稳定性。

## 下载

- `Coomi-Android-arm64-v1.3.2.apk`
- 架构：ARM64 (`arm64-v8a`)
- 系统要求：Android 7.0 及以上
